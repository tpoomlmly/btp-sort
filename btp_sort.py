from typing import TypeVar, Iterable

T = TypeVar('T')

def btp_sort(
    iterable: Iterable[T]  # See it
) -> Iterable[T]:
    """Sort an iterable using the British Transport Police sorting algorithm."""
    print(iterable)  # Say it
    return(iterable)  # Sorted
